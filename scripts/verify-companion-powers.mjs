import {chromium} from 'file:///C:/Users/black/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';
import assert from 'node:assert/strict';
const browser=await chromium.launch({channel:'msedge',headless:true}),page=await browser.newPage({viewport:{width:390,height:844},isMobile:true,hasTouch:true}),errors=[];page.on('pageerror',e=>errors.push(e.message));
try{await page.addInitScript(()=>localStorage.setItem('3b-origins-v1:guest',JSON.stringify({zone:'maroc',position:{x:-48,z:-33},avatar:{created:true,name:'Test soutien',companion:'silver',weapon:'zellige'},flags:{awakened:true},settings:{quality:'light'}})));await page.goto('http://127.0.0.1:4173/#monde-3b');await page.getByRole('button',{name:'Reprendre l’aventure',exact:true}).click({timeout:60000});await page.locator('.origins-interact').click();await page.waitForTimeout(300);const energy=()=>page.getByRole('meter',{name:'Énergie du Cercle'}).getAttribute('value').then(Number),before=await energy(),button=page.getByRole('button',{name:'Commander le loup'});await button.click();await page.waitForTimeout(300);assert.ok(await energy()<before-12);assert.equal(await button.isDisabled(),true);await page.screenshot({path:'artifacts/refonte/companion-support-mobile.png'});assert.deepEqual(errors,[]);console.log('PASS mobile combat: guard support costs synergy energy, shared cooldown displayed, no runtime errors');}finally{await browser.close();}
