import {chromium} from 'file:///C:/Users/black/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';
import assert from 'node:assert/strict';
const browser=await chromium.launch({channel:'msedge',headless:true}),page=await browser.newPage({viewport:{width:390,height:844},isMobile:true,hasTouch:true}),errors=[];page.on('pageerror',e=>errors.push(e.message));
try{await page.addInitScript(()=>localStorage.setItem('3b-origins-v1:guest',JSON.stringify({zone:'maroc',position:{x:-48,z:-33},avatar:{created:true,name:'Test commandes'},flags:{awakened:true},settings:{quality:'light'}})));await page.goto('http://127.0.0.1:4173/#monde-3b');await page.getByRole('button',{name:'Reprendre l’aventure',exact:true}).click({timeout:60000});await page.locator('.origins-interact').click();await page.getByText('Attaque annoncée · esquive !',{exact:true}).waitFor({timeout:15000});const hp=()=>page.getByRole('meter',{name:'Vie de l’adversaire'}).getAttribute('value').then(Number),before=await hp();const attack=page.getByRole('button',{name:'Attaque rapide',exact:true});await attack.dispatchEvent('pointerdown',{button:0,pointerType:'touch',pointerId:99});await page.waitForTimeout(185);await attack.dispatchEvent('pointerdown',{button:0,pointerType:'touch',pointerId:99});await page.waitForTimeout(410);assert.equal(before-await hp(),26);await page.screenshot({path:'artifacts/refonte/combat-chain-mobile.png'});assert.deepEqual(errors,[]);console.log('PASS: two touch presses produce exactly two hits; queued attack and runtime verified');}finally{await browser.close();}
