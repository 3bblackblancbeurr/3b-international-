import {chromium} from 'file:///C:/Users/black/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright/index.mjs';
import assert from 'node:assert/strict';
const browser=await chromium.launch({channel:'msedge',headless:true}),p=await browser.newPage({viewport:{width:1280,height:900}}),errors=[];p.on('pageerror',e=>errors.push(e.message));
try{await p.goto((process.env.WORLD_TEST_URL||'http://127.0.0.1:4173')+'/#arene');await p.getByRole('button',{name:/Entraînement contre l’ordinateur/}).click({timeout:60000});await p.getByRole('button',{name:/Frapper/}).click();await p.waitForTimeout(1500);await p.screenshot({path:'artifacts/refonte/arena-release-duel.png'});await p.getByRole('button',{name:'Abandonner le duel',exact:true}).click();await p.getByRole('button',{name:'Rejouer',exact:true}).waitFor();await p.waitForTimeout(500);await p.screenshot({path:'artifacts/refonte/arena-release-result.png'});assert.deepEqual(errors,[]);await p.getByRole('button',{name:'Rejouer',exact:true}).click();await p.getByRole('button',{name:/Frapper/}).waitFor();console.log('Arena practice: strike, opponent response, forfeit, result and replay passed');}finally{await browser.close();}
