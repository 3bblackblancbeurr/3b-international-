export const AUDIO_BUSES=Object.freeze(['engine','turbo','transmission','tires','collision','weather','crowd','police','radio','music','ui']);
export function createAudioState(){return {musicIntensity:0,policeIntensity:0,weatherIntensity:0,engineLoad:0,lastCue:null};}
export function updateAudioDirector(state,{speedKph=0,rpm=1000,throttle=0,heat=0,wetness=0,position=1,finalLap=false,eventState='race'}={}){const engineLoad=Math.min(1,throttle*.7+rpm/8000*.3),policeIntensity=Math.min(1,heat/6),weatherIntensity=Math.min(1,wetness),racePressure=Math.min(1,(position>1?.35:.15)+(finalLap?.4:0)+(speedKph>220?.15:0)),musicIntensity=eventState==='race'?Math.min(1,racePressure+policeIntensity*.35):policeIntensity*.4;let lastCue=state.lastCue;if(finalLap&&state.lastCue!=='final-lap')lastCue='final-lap';else if(heat>=5&&state.policeIntensity<.8)lastCue='high-heat';return {...state,engineLoad,policeIntensity,weatherIntensity,musicIntensity,lastCue};}
export function audioMix(state){return {engine:.65+state.engineLoad*.35,turbo:state.engineLoad,transmission:.75,tires:.55+state.weatherIntensity*.25,collision:.9,weather:state.weatherIntensity,crowd:.45,police:state.policeIntensity,radio:state.policeIntensity>.2?.75:.25,music:.55+state.musicIntensity*.35,ui:.8};}
