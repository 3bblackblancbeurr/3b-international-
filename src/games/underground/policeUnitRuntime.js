import {pursuitDrivingCommand,safeContactIntent} from './policeDrivingAI.js';import {pursuitCollisionPolicy,trafficRiskAround} from './policeTrafficInteraction.js';
export function createPoliceUnit({id,type='patrol',topSpeedKph=250,position={x:0,z:0}}={}){return {id,type,topSpeedKph,position,forward:{x:0,z:1},speedKph:0,state:'deploying',health:100,route:[],routeIndex:0,tactic:'follow',lostSeconds:0};}
export function tickPoliceUnit(unit,{player,routeTarget,traffic=[],tactic=unit.tactic,dt=.016}={}){const trafficInfo=trafficRiskAround(unit.position,traffic),policy=pursuitCollisionPolicy({trafficRisk:trafficInfo.risk,heat:player.heat||1}),drive=pursuitDrivingCommand({unit,player,routeTarget,tactic,trafficRisk:trafficInfo.risk,dt}),relative=(unit.speedKph||0)-(player.speedKph||0),contact=safeContactIntent({tactic,relativeSpeedKph:relative,angleDeg:0,playerSpeedKph:player.speedKph});return {...unit,tactic,state:'pursuing',drive,contactIntent:policy.abortContact?{contact:false}:contact,trafficPolicy:policy};}
export function damagePoliceUnit(unit,amount){const health=Math.max(0,unit.health-Math.max(0,amount));return {...unit,health,state:health<=0?'disabled':unit.state};}
