export const PLATFORM_BUDGETS=Object.freeze({desktopUltra:{targetFps:60,maxDrawCalls:1800,maxVisibleVehicles:42,maxTextureMb:3072,shadowDistance:180,worldLodScale:1},desktopHigh:{targetFps:60,maxDrawCalls:1300,maxVisibleVehicles:32,maxTextureMb:2048,shadowDistance:140,worldLodScale:.85},mobileHigh:{targetFps:60,maxDrawCalls:750,maxVisibleVehicles:20,maxTextureMb:1024,shadowDistance:90,worldLodScale:.62},mobileSafe:{targetFps:45,maxDrawCalls:480,maxVisibleVehicles:14,maxTextureMb:640,shadowDistance:55,worldLodScale:.45}});
export function choosePerformanceProfile({memoryGb=8,cores=8,width=1920,dpr=1,mobile=false}={}){if(mobile)return memoryGb>=8&&cores>=8?'mobileHigh':'mobileSafe';if(memoryGb>=16&&cores>=12&&width*dpr>=2500)return'desktopUltra';return'desktopHigh';}
export function adaptivePerformanceState({profile='desktopHigh',frameMs=16.7,traffic=1,fx=1,shadows=1}={}){const b=PLATFORM_BUDGETS[profile],target=1000/b.targetFps,pressure=frameMs/target;return {profile,traffic:Math.max(.5,Math.min(1,pressure>1.25?traffic-.12:pressure<.9?traffic+.05:traffic)),fx:Math.max(.45,Math.min(1,pressure>1.35?fx-.16:pressure<.88?fx+.05:fx)),shadows:Math.max(.4,Math.min(1,pressure>1.2?shadows-.1:shadows)),budget:b};}
