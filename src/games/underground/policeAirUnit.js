const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
export function createAirUnit({id='air-1',position={x:0,y:80,z:0}}={}){return {id,position,state:'tracking',battery:100,spotConfidence:0};}
export function tickAirUnit(unit,{player,weatherVisibility=1,dt=.016}={}){const target={x:player.position.x+(player.velocity?.x||0)*1.5,y:clamp(65+(player.speedKph||0)*.08,65,105),z:player.position.z+(player.velocity?.z||0)*1.5},dx=target.x-unit.position.x,dy=target.y-unit.position.y,dz=target.z-unit.position.z,k=Math.min(1,dt*.8),position={x:unit.position.x+dx*k,y:unit.position.y+dy*k,z:unit.position.z+dz*k},distance=Math.hypot(dx,dz),confidence=clamp(weatherVisibility*(1-distance/900),0,1),battery=Math.max(0,unit.battery-dt*.12);return {...unit,position,spotConfidence:confidence,battery,state:battery<=5?'returning':confidence>.25?'tracking':'searching'};}
