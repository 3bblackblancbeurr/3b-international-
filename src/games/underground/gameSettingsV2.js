export const DEFAULT_GAME_SETTINGS=Object.freeze({difficulty:'normal',transmission:'automatic',tractionControl:.65,abs:.75,stabilityAssist:.55,steeringAssist:.2,damage:'visual-and-performance',cameraShake:.55,motionBlur:.5,subtitles:true,colorBlind:'off',hudScale:1,audioDynamicRange:'wide',policeDifficulty:'adaptive'});
export const DIFFICULTY_PRESETS=Object.freeze({story:{ai:.82,rewards:1.08,police:.78,damage:.7},normal:{ai:.9,rewards:1,police:1,damage:1},hard:{ai:.95,rewards:1.12,police:1.12,damage:1.08},legend:{ai:.985,rewards:1.28,police:1.25,damage:1.18}});
export function normalizeGameSettings(value={}){const s={...DEFAULT_GAME_SETTINGS,...value};s.tractionControl=Math.max(0,Math.min(1,Number(s.tractionControl)||0));s.abs=Math.max(0,Math.min(1,Number(s.abs)||0));s.stabilityAssist=Math.max(0,Math.min(1,Number(s.stabilityAssist)||0));s.steeringAssist=Math.max(0,Math.min(1,Number(s.steeringAssist)||0));s.cameraShake=Math.max(0,Math.min(1,Number(s.cameraShake)||0));s.motionBlur=Math.max(0,Math.min(1,Number(s.motionBlur)||0));s.hudScale=Math.max(.75,Math.min(1.4,Number(s.hudScale)||1));if(!DIFFICULTY_PRESETS[s.difficulty])s.difficulty='normal';return s;}
export function difficultyConfig(settings){const s=normalizeGameSettings(settings);return {...DIFFICULTY_PRESETS[s.difficulty],id:s.difficulty};}
