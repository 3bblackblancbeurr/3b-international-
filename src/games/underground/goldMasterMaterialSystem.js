import {GOLD_MASTER_BY_ID} from './productionCatalog.js';
const LIB=Object.freeze({metal:{metallic:.9,roughness:.24},steel:{metallic:.88,roughness:.3},aluminium:{metallic:.86,roughness:.22},copper:{metallic:.95,roughness:.28},bronze:{metallic:.92,roughness:.32},glass:{metallic:.05,roughness:.08,transmission:.88},stone:{metallic:.02,roughness:.72},marble:{metallic:.04,roughness:.34},wood:{metallic:.01,roughness:.58},textile:{metallic:0,roughness:.8},ceramic:{metallic:.02,roughness:.24},carbon:{metallic:.58,roughness:.26},mineral:{metallic:.08,roughness:.62}});
function family(name=''){const n=name.toLowerCase();if(/verre|glass|cristal/.test(n))return'glass';if(/pierre|stone|granit|calcaire|ardoise/.test(n))return'stone';if(/bois|wood|chêne/.test(n))return'wood';if(/tissu|textile|dentelle|velours/.test(n))return'textile';if(/céramique|ceramic|porcelaine/.test(n))return'ceramic';if(/carbone|carbon/.test(n))return'carbon';if(/cuivre|copper/.test(n))return'copper';if(/bronze/.test(n))return'bronze';if(/acier|steel|fer/.test(n))return'steel';if(/marbre|marble/.test(n))return'marble';if(/aluminium/.test(n))return'aluminium';return'mineral';}
export function goldMasterMaterialRecipe(vehicleId){const spec=GOLD_MASTER_BY_ID[vehicleId];if(!spec)return null;return {vehicleId,layers:(spec.materials||[]).map((name,index)=>{const f=family(name);return {name,channel:index===0?'bodyPrimary':index===1?'bodySecondary':`accent${index-1}`,family:f,params:{...LIB[f]}};}),requiresPbr:['baseColor','normal','roughness','metallic'],premium:true};}
