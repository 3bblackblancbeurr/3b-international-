const clamp=(v,a,b)=>Math.max(a,Math.min(b,v));
export const RIVAL_STYLES=Object.freeze({clean:{aggression:.28,risk:.3,defense:.45,error:.025},attacker:{aggression:.82,risk:.72,defense:.55,error:.065},defender:{aggression:.5,risk:.38,defense:.88,error:.035},drifter:{aggression:.58,risk:.78,defense:.42,error:.07},calculator:{aggression:.38,risk:.25,defense:.7,error:.018},sprinter:{aggression:.7,risk:.62,defense:.4,error:.055}});
export function adaptiveSkill({base=.8,playerWinRate=.5,streak=0,difficulty=.5,boss=false}={}){const rubber=(playerWinRate-.5)*.12+Math.min(5,Math.max(-5,streak))*.008;return clamp(base+difficulty*.08+rubber+(boss?.045:0),.65,.995);}
export function rivalDecision({style='clean',skill=.8,relativePositionM=0,cornerSharpness=.3,traffic=.3,wetness=0,seed=.5}={}){const s=RIVAL_STYLES[style]||RIVAL_STYLES.clean,risk=s.risk*(1-wetness*.35)*(1-traffic*.2),errorChance=s.error*(1-skill*.55),mistake=seed<errorChance;let action=relativePositionM>15?'attack':relativePositionM<-10?'defend':'pace';if(cornerSharpness>.7&&risk>.55)action='late-brake';if(mistake)action='mistake';return {action,targetPace:clamp(skill*(.94+risk*.08),.6,1.03),aggression:s.aggression,defense:s.defense,mistake};}
export function bossPhases({progress=0,health=1}={}){if(progress<.33)return {phase:'measure',pace:1,aggression:.45};if(progress<.72)return {phase:'pressure',pace:1.035,aggression:.68};return {phase:'final',pace:health<.45?1.02:1.055,aggression:.82};}
