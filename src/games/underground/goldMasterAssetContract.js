import {GOLD_MASTER_BY_ID} from './productionCatalog.js';
export const GOLD_MASTER_LODS=Object.freeze([{id:'lod0',maxDistanceM:18,maxTriangles:180000},{id:'lod1',maxDistanceM:45,maxTriangles:90000},{id:'lod2',maxDistanceM:110,maxTriangles:35000},{id:'lod3',maxDistanceM:Infinity,maxTriangles:9000}]);
export const REQUIRED_PBR=Object.freeze(['baseColor','normal','roughness','metallic']);
export function expectedGoldMasterBundle(vehicleId){const spec=GOLD_MASTER_BY_ID[vehicleId];if(!spec)return null;const root=spec.expectedAsset.replace(/\/[^/]+$/,'');return {vehicleId,root,model:spec.expectedAsset,collision:`${root}/collision.glb`,cockpit:`${root}/cockpit.glb`,lod:GOLD_MASTER_LODS.map(x=>({...x,asset:`${root}/${x.id}.glb`})),pbr:Object.fromEntries(REQUIRED_PBR.map(k=>[k,`${root}/pbr/${k}.webp`]))};}
export function validateGoldMasterBundle(vehicleId,bundle={}){const expected=expectedGoldMasterBundle(vehicleId);if(!expected)return {ok:false,errors:['unknown-gold-master']};const errors=[];if(bundle.model!==expected.model)errors.push('model-path');if(!bundle.collision)errors.push('collision');if(!bundle.cockpit)errors.push('cockpit');for(const k of REQUIRED_PBR)if(!bundle.pbr?.[k])errors.push(`pbr:${k}`);if(!Array.isArray(bundle.lod)||bundle.lod.length<4)errors.push('lod-count');return {ok:errors.length===0,errors,expected};}
