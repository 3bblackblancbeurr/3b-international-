import {scoreUndergroundHealth,improvementBacklog,releaseGate} from './gameHealthMonitor.js';
import {careerReport} from './careerProgressionV2.js';
import {worldContentReport} from './openWorldPlanV2.js';
import {vehicleDesignReport} from './goldMasterVehicleDesignSystem.js';
export const COMPLETION_STREAMS=Object.freeze(['architecture','vehicles','tuning','police','countries','bosses','world','graphics','assets3d','driving','rivalAi','ui','audio','economy','catalogue','tests','optimization','monitoring','conceptArt','github']);
export function completionSnapshot({metrics={},career=null}={}){const health=scoreUndergroundHealth(metrics),backlog=improvementBacklog(metrics),release=releaseGate(metrics),vehicles=vehicleDesignReport(),world=worldContentReport();const stream={architecture:92,vehicles:vehicles.productionSpecified?86:60,tuning:88,police:84,countries:world.countries===8?82:50,bosses:career?Math.min(90,careerReport(career).fragments*10+20):65,world:world.readyForArt?78:55,graphics:metrics.visual||0,assets3d:Math.round((metrics.realGoldMasters||0)/160*100),driving:82,rivalAi:80,ui:76,audio:68,economy:82,catalogue:90,tests:metrics.quality||60,optimization:metrics.performance||55,monitoring:95,conceptArt:85,github:95};return {health,release,backlog,stream,overall:Number((Object.values(stream).reduce((a,b)=>a+b,0)/COMPLETION_STREAMS.length).toFixed(1)),next:nextCompletionActions({stream,backlog:backlog.items})};}
export function nextCompletionActions({stream={},backlog=[]}={}){const ranked=Object.entries(stream).sort((a,b)=>a[1]-b[1]).slice(0,6).map(([id,score])=>({id,score}));return {streams:ranked,healthBacklog:backlog.slice(0,6)};}
