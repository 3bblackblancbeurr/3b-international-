export function raceHudModel({speedKph=0,gear=1,rpm=0,nitrous=0,position=1,total=8,lap=1,laps=1,objective='',heat=0,damage=0,cooldown=0,minimap=null}={}){return {speedKph:Math.round(speedKph),gear,rpm:Math.round(rpm),nitrous:Math.max(0,Math.min(1,nitrous)),position,total,lap,laps,objective,heat,damage:Math.round(damage),cooldown:Math.ceil(cooldown),minimap,alerts:[...(damage>80?['critical-damage']:[]),...(heat>=5?['high-heat']:[]),...(nitrous<.1?['nitrous-low']:[])]};}
export function worldHudModel({country,city,level=1,xp=0,nextXp=1000,coins=0,influence=0,fragment=false,nearbyEvents=[]}={}){return {country,city,level,xp,progress:nextXp?Math.min(1,xp/nextXp):1,coins,influence,fragment,nearbyEvents:nearbyEvents.slice(0,5)};}
