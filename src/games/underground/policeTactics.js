const hash=(seed,n)=>{let x=(seed*1664525+n*1013904223)>>>0;x^=x<<13;x^=x>>>17;x^=x<<5;return (x>>>0)/4294967295;};
export const UNIT_TYPES=Object.freeze({patrol:{speedBias:0,weight:1},interceptor:{speedBias:45,weight:.8},heavy:{speedBias:-25,weight:1.8},tracker:{speedBias:15,weight:.9},air:{speedBias:120,weight:.3}});
export function choosePoliceTactic({heat=1,playerSpeedKph=0,roadWidthM=12,traffic=.4,seed=1,sequence=0}={}){const r=hash(seed,sequence),choices=heat<=2?['follow','intercept']:heat<=4?['intercept','box','roadblock','predict']:['predict','box','roadblock','split-intercept','air-track'];let tactic=choices[Math.floor(r*choices.length)];if(roadWidthM<7&&tactic==='box')tactic='follow';if(traffic>.75&&tactic==='roadblock')tactic='predict';if(playerSpeedKph>220&&tactic==='follow')tactic='intercept';return tactic;}
export function dispatchComposition(heat=0){if(heat<=0)return [];const units=['patrol'];if(heat>=2)units.push('interceptor');if(heat>=3)units.push('tracker');if(heat>=4)units.push('heavy');if(heat>=5)units.push('air');return units;}
export function predictIntercept({playerPosition,playerVelocity,seconds=3}={}){return {x:(playerPosition?.x||0)+(playerVelocity?.x||0)*seconds,z:(playerPosition?.z||0)+(playerVelocity?.z||0)*seconds};}
export function roadblockPlan({heat=1,roadWidthM=12,laneCount=2,escapeMarginM=2.8}={}){const blockers=Math.min(Math.max(1,Math.floor(heat/2)+1),Math.max(1,laneCount));return {blockers,leaveEscapeGap:true,escapeGapM:escapeMarginM,spikeStrip:heat>=4&&roadWidthM>=8,heavyUnit:heat>=5};}
