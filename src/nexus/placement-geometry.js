export function rotatedFootprint(footprint=[1,1],rotation=0){const w=Math.max(1,Number(footprint[0])||1),d=Math.max(1,Number(footprint[1])||1),rad=((((Number(rotation)||0)%360)+360)%360)*Math.PI/180,c=Math.abs(Math.cos(rad)),s=Math.abs(Math.sin(rad));return[Math.max(1,Math.ceil(w*c+d*s-1e-10)),Math.max(1,Math.ceil(w*s+d*c-1e-10))];}
export function footprintInsideBounds({x=0,z=0,footprint=[1,1],rotation=0},half=20){const[w,d]=rotatedFootprint(footprint,rotation);return Math.abs(Number(x)||0)+w/2<=half&&Math.abs(Number(z)||0)+d/2<=half;}
export function footprintsOverlap(a,b){const[aw,ad]=rotatedFootprint(a.footprint,a.rotation),[bw,bd]=rotatedFootprint(b.footprint,b.rotation);return Math.abs((Number(a.x)||0)-(Number(b.x)||0))<=(aw+bw)/2&&Math.abs((Number(a.z)||0)-(Number(b.z)||0))<=(ad+bd)/2;}
