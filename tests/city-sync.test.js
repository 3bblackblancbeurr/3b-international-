import test from 'node:test';import assert from 'node:assert/strict';import {blankCity} from '../src/nexus/city-save.js';import {createSyncState,queueCityOp,materializeSyncCity,reconcileCityConflict,acknowledgeSync,syncPayload} from '../src/nexus/city-sync.js';
test('pending city operations materialize locally',()=>{let s=createSyncState(blankCity('x'));s=queueCityOp(s,{type:'place',id:'a',building:'3b-home-small',x:0,z:0,rotation:0});assert.equal(materializeSyncCity(s).placements.length,1);assert.equal(syncPayload(s).operations.length,1);});
test('conflict rebases pending operations on server snapshot',()=>{let s=createSyncState(blankCity('x'));s=queueCityOp(s,{type:'place',id:'a',building:'3b-home-small',x:20,z:20,rotation:0});const remote={...blankCity('x'),revision:4,currency:800};const r=reconcileCityConflict(s,remote);assert.equal(r.serverRevision,4);assert.equal(r.base.placements.length,1);assert.equal(r.conflict,true);});
test('acknowledge clears pending operations',()=>{let s=createSyncState(blankCity('x'));s=queueCityOp(s,{type:'place',id:'a',building:'3b-home-small',x:0,z:0,rotation:0});s=acknowledgeSync(s,{...blankCity('x'),revision:2});assert.equal(s.pending.length,0);assert.equal(s.serverRevision,2);});
